include: deepin-desktop-pkgs.yaml
ref: fedora/31/${basearch}/deepin
rojig:
  name: fedora-deepin
  summary: "Fedora Deepin base image"
  license: MIT

check-passwd: { type: file, filename: passwd.deepin }
check-groups: { type: file, filename: group.deepin }
preserve-passwd: true

packages:

repos:
  - fedora-31
  - fedora-31-updates
