include: xfce-desktop-pkgs.yaml
ref: fedora/31/${basearch}/xfce
rojig:
  name: fedora-xfce
  summary: "Fedora XFCE base image"
  license: MIT

check-passwd: { type: file, filename: passwd.xfce }
check-groups: { type: file, filename: group.xfce }
preserve-passwd: true

packages:
  # Portals for Flatpak
  - xdg-desktop-portal-gtk
repos:
  - fedora-31
  - fedora-31-updates
